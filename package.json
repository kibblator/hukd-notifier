{
  "name": "dg.dealalerts",
  "version": "1.0.0",
  "description": "A repository for alerting the user to new deals",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "start:dev": "env NODE_ENV=development nodemon --watch 'src/**/*.ts' --exec ts-node src/index.ts",
    "start": "env NODE_ENV=production nodemon --watch 'src/**/*.ts' --exec ts-node src/index.ts",
    "test": "npm test"
  },
  "author": "kibblator",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^20.7.1",
    "ts-node": "^10.9.1",
    "typescript": "^5.2.2"
  },
  "dependencies": {
    "@types/body-parser": "^1.19.3",
    "@types/dotenv": "^8.2.0",
    "@types/express": "^4.17.18",
    "axios": "^1.5.1",
    "body-parser": "^1.20.2",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "lokijs": "^1.5.12",
    "node-cron": "^3.0.3",
    "nodemon": "^3.0.1",
    "xml2js": "^0.6.2"
  }
}
